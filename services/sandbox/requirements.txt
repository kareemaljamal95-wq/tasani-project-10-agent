fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Available to the code a run executes, not used by the service itself.
# Pinned exactly: pandas changes default behaviour between minor releases, and
# an unpinned image means a rerun months later produces a different file from
# the one the customer accepted, with nothing to show which was right.
pandas==2.2.3
openpyxl==3.1.5
pytest==8.3.4
